import styled from 'styled-components/native';
import {Picker, Card} from 'native-base';

export const Container = styled(Card)`
  flex: 1;
  padding: 0 15px;
  max-height: 46px;
  border-radius: 4px;
  flex-direction: row;
  align-items: center;
`;

export const CPicker = styled(Picker)``;
